import React from 'react';
import Layout from './Hoc/Layout';
import { Switch, Route } from 'react-router-dom';

const Routes = (props) => {
	return (
		<Layout>
			<Switch>second line</Switch>
		</Layout>
	);
};

export default Routes;
